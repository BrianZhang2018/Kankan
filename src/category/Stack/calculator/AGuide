题的递进：

BasicCalculatorAllOpsNoParentheses -> BasicCalculatorPlusDivideParentheses -> BasicCalculatorAllOpsParentheses

解题思路：

template:

1. prevSign - track previous sign
2. tmpNumber - the number between the sign
3. common way calculate the number
     if (i == s.length() - 1 || (signs.indexOf(c)!=-1)) { // calculate the number before current sign = prevSign * tmpNum
                    if (prevSign == '-') {
                        stack.push(-tmpNum);
                    } else if (prevSign == '+') {
                        stack.push(tmpNum);
                    } else if (prevSign == '*') {
                        stack.push(stack.pop() * tmpNum);
                    } else if (prevSign == '/') {
                        stack.push(stack.pop() / tmpNum);
                    }
                    prevSign = c; //
                    tmpNum = 0;
                }